import test from 'node:test';
import assert from 'node:assert/strict';
import { classifyRaceQueryIntent, evaluateRaceCommand, normalizeRaceCommandInput, type RaceCommandInput, type RaceLifecycleState, type RaceResultStage } from './race-lifecycle.js';
const command=(state:RaceLifecycleState,patch:Partial<RaceCommandInput>={}):RaceCommandInput=>({command:'ANNOUNCE',expectedState:state,requestId:'request-0001',idempotencyKey:'idem-0000001',actorId:'operator-1',actorType:'operator',correlationId:'trace-000001',evidence:[],payload:{},...patch});
void test('canonical happy path keeps observed provisional verified and official distinct',()=>{let state:RaceLifecycleState='DISCOVERED',stage:RaceResultStage='none';const apply=(input:RaceCommandInput)=>{const result=evaluateRaceCommand(state,input,stage);assert.equal(result.allowed,true,result.reason);state=result.to;stage=result.resultStage;return result;};apply(command(state,{command:'ANNOUNCE'}));apply(command(state,{command:'OPEN',expectedState:state}));apply(command(state,{command:'BEGIN_CLOSING',expectedState:state}));apply(command(state,{command:'CLOSE',expectedState:state}));assert.equal(state,'CLOSED');assert.equal(stage,'none');apply(command(state,{command:'RECORD_OBSERVED_ARRIVAL',expectedState:state,payload:{arrival:['5','3','1']}}));assert.equal(stage,'observed');apply(command(state,{command:'START',expectedState:state}));apply(command(state,{command:'RECORD_PROVISIONAL_RESULT',expectedState:state,payload:{arrival:['5','3','1']}}));assert.equal(stage,'provisional');apply(command(state,{command:'MARK_VERIFIED_RESULT',expectedState:state,evidence:[{source:'trusted-1',authority:'trusted',confidence:.95},{source:'trusted-2',authority:'trusted',confidence:.93}]}));assert.equal(stage,'verified');apply(command(state,{command:'MARK_OFFICIAL_RESULT',expectedState:state,evidence:[{source:'official-feed',authority:'official',confidence:.99}]}));assert.equal(state,'OFFICIAL_RESULT');assert.equal(stage,'official');apply(command(state,{command:'ARCHIVE',expectedState:state}));assert.equal(state,'ARCHIVED');assert.equal(stage,'official');});
void test('cancelled race can be archived without fabricating an official result stage',()=>{const cancelled=evaluateRaceCommand('POSTPONED',command('POSTPONED',{command:'CANCEL'}),'none');assert.equal(cancelled.allowed,true);assert.equal(cancelled.to,'CANCELLED');assert.equal(cancelled.resultStage,'none');const archived=evaluateRaceCommand('CANCELLED',command('CANCELLED',{command:'ARCHIVE'}),cancelled.resultStage);assert.equal(archived.allowed,true);assert.equal(archived.to,'ARCHIVED');assert.equal(archived.resultStage,'none');});
void test('CLOSED never implies settlement or result authority',()=>{const result=evaluateRaceCommand('OPEN',command('OPEN',{command:'CLOSE'}),'none');assert.equal(result.allowed,true);assert.equal(result.to,'CLOSED');assert.equal(result.resultStage,'none');});
void test('one ambiguous trusted signal cannot auto-open a race',()=>{const denied=evaluateRaceCommand('ANNOUNCED',command('ANNOUNCED',{command:'OPEN',actorType:'agent',evidence:[{source:'message-1',authority:'trusted',confidence:.91}]}));assert.equal(denied.allowed,false);assert.equal(denied.reason,'OPEN_REQUIRES_CORROBORATED_EVIDENCE');const allowed=evaluateRaceCommand('ANNOUNCED',command('ANNOUNCED',{command:'OPEN',actorType:'agent',evidence:[{source:'message-1',authority:'trusted',confidence:.91},{source:'provider-1',authority:'official',confidence:.95}]}));assert.equal(allowed.allowed,true);});
void test('arrival/provisional/verified wording cannot become official without official evidence',()=>{const denied=evaluateRaceCommand('PROVISIONAL_RESULT',command('PROVISIONAL_RESULT',{command:'MARK_OFFICIAL_RESULT',evidence:[{source:'provider',authority:'trusted',confidence:.99}]}),'verified');assert.equal(denied.allowed,false);assert.equal(denied.reason,'OFFICIAL_RESULT_REQUIRES_OFFICIAL_EVIDENCE');assert.equal(denied.resultStage,'verified');});
void test('data conflict is an explicit audit-only event requiring two distinct evidence sources',()=>{const denied=evaluateRaceCommand('OPEN',command('OPEN',{command:'RECORD_DATA_CONFLICT',evidence:[{source:'provider-1',authority:'trusted',confidence:.95}],payload:{conflictType:'RESULT',candidates:['A','B']}}),'none');assert.equal(denied.allowed,false);assert.equal(denied.reason,'DATA_CONFLICT_REQUIRES_MULTIPLE_SOURCES');const missingCandidates=evaluateRaceCommand('OPEN',command('OPEN',{command:'RECORD_DATA_CONFLICT',evidence:[{source:'provider-1',authority:'trusted',confidence:.95},{source:'document-1',authority:'official',confidence:.99}],payload:{conflictType:'RESULT'}}),'none');assert.equal(missingCandidates.allowed,false);assert.equal(missingCandidates.reason,'DATA_CONFLICT_EVIDENCE_REQUIRED');const recorded=evaluateRaceCommand('OPEN',command('OPEN',{command:'RECORD_DATA_CONFLICT',evidence:[{source:'provider-1',authority:'trusted',confidence:.95},{source:'document-1',authority:'official',confidence:.99}],payload:{conflictType:'RESULT',candidates:['A','B']}}),'none');assert.equal(recorded.allowed,true);assert.equal(recorded.reason,'DATA_CONFLICT');assert.equal(recorded.to,'OPEN');assert.equal(recorded.resultStage,'none');});
void test('idempotency key is explicit and defaults to requestId only for backwards compatibility',()=>{const explicit=normalizeRaceCommandInput(command('OPEN',{command:'CLOSE',requestId:'request-1000',idempotencyKey:'retry-key-1000'}));assert.equal(explicit.idempotencyKey,'retry-key-1000');const fallback=normalizeRaceCommandInput({...command('OPEN',{command:'CLOSE',requestId:'request-2000'}),idempotencyKey:undefined});assert.equal(fallback.idempotencyKey,'request-2000');});
void test('expected-state mismatch fails closed',()=>{const result=evaluateRaceCommand('PROVISIONAL_RESULT',command('ANNOUNCED',{command:'MARK_OFFICIAL_RESULT'}),'verified');assert.equal(result.allowed,false);assert.equal(result.reason,'EXPECTED_STATE_MISMATCH');assert.equal(result.resultStage,'verified');});
void test('postponed suspended and cancelled paths are explicit',()=>{assert.equal(evaluateRaceCommand('ANNOUNCED',command('ANNOUNCED',{command:'POSTPONE'})).to,'POSTPONED');assert.equal(evaluateRaceCommand('POSTPONED',command('POSTPONED',{command:'RESUME'})).to,'ANNOUNCED');assert.equal(evaluateRaceCommand('OPEN',command('OPEN',{command:'SUSPEND'})).to,'SUSPENDED');assert.equal(evaluateRaceCommand('SUSPENDED',command('SUSPENDED',{command:'CANCEL'})).to,'CANCELLED');});
void test('live query intents cover required operational questions and unknown is not guessed',()=>{assert.equal(classifyRaceQueryIntent('¿Cuál es la próxima carrera?'),'NEXT_RACE');assert.equal(classifyRaceQueryIntent('carrera activa'),'ACTIVE_RACE');assert.equal(classifyRaceQueryIntent('última pizarra'),'LAST_RESULT');assert.equal(classifyRaceQueryIntent('resultado de la carrera'),'RESULT');assert.equal(classifyRaceQueryIntent('¿ese resultado es oficial?'),'OFFICIALITY');assert.equal(classifyRaceQueryIntent('programación de hoy'),'SCHEDULE');assert.equal(classifyRaceQueryIntent('¿a qué hora es la carrera?'),'SCHEDULED_TIME');assert.equal(classifyRaceQueryIntent('ejemplares inscritos'),'RUNNERS');assert.equal(classifyRaceQueryIntent('retirados'),'SCRATCHES');assert.equal(classifyRaceQueryIntent('momios de la carrera'),'ODDS');assert.equal(classifyRaceQueryIntent('estado de la reunión'),'MEETING_STATUS');assert.equal(classifyRaceQueryIntent('dime cualquier cosa'),'UNKNOWN');});
