name: Hípico Shadow Metrics v7

on:
  pull_request:
    paths:
      - 'backend/src/modules/hipico/agent-policy.ts'
      - 'backend/src/modules/hipico/agent-policy.test.ts'
      - 'backend/src/modules/hipico/agent-promotion-metrics.test.ts'
      - 'backend/src/modules/hipico/agent-golden.ts'
      - 'backend/src/modules/hipico/agent-golden.test.ts'
      - 'backend/src/modules/hipico/automation.store.ts'
      - 'backend/src/modules/hipico/agent.routes.ts'
      - 'backend/src/modules/hipico/hipico-agent.integration.ts'
      - 'backend/src/modules/hipico/promotion-snapshot.test.ts'
      - 'backend/src/modules/hipico/shadow-metrics*.ts'
      - 'backend/src/modules/hipico/decision-provider*.ts'
      - 'backend/src/modules/hipico/jev-decision-provider*.ts'
      - 'backend/prisma/migrations/20260914194000_hipico_shadow_metrics_v7/migration.sql'
      - 'supabase/sql/hipico_v24_shadow_metrics.sql'
      - '.github/workflows/hipico-shadow-metrics-v7.yml'
  workflow_dispatch:

permissions: { contents: read }

env:
  HIPICO_CANDIDATE_SHA: ${{ github.event.pull_request.head.sha || github.sha }}

concurrency:
  group: hipico-shadow-metrics-v7-${{ github.event.pull_request.number || github.ref }}
  cancel-in-progress: true

jobs:
  shadow-metrics-promotion-gates:
    runs-on: ubuntu-latest
    timeout-minutes: 35
    services:
      postgres:
        image: postgres:16
        env:
          POSTGRES_PASSWORD: postgres
          POSTGRES_USER: postgres
          POSTGRES_DB: postgres
        ports: ['5432:5432']
        options: >-
          --health-cmd "pg_isready -U postgres -d postgres"
          --health-interval 5s --health-timeout 5s --health-retries 20
    steps:
      - name: Checkout exact candidate
        uses: actions/checkout@v7
        with:
          ref: "${{ env.HIPICO_CANDIDATE_SHA }}"
          fetch-depth: 0

      - name: Exact candidate SHA and clean-tree gate
        run: node scripts/hipico-exact-sha-gate.mjs

      - name: Configure Node 22
        uses: actions/setup-node@v7
        with:
          node-version: '22'
          cache: npm
          cache-dependency-path: package-lock.json

      - name: Install lockfile dependencies
        run: npm ci --no-audit --no-fund

      - name: Install PostgreSQL client
        run: sudo apt-get update && sudo apt-get install -y --no-install-recommends postgresql-client

      - name: Create isolated ephemeral PostgreSQL database
        shell: bash
        env:
          PGPASSWORD: postgres
        run: |
          set -euo pipefail
          db="hipico_agent_e2e_${GITHUB_RUN_ID}_${GITHUB_RUN_ATTEMPT}"; db="${db,,}"
          createdb -h 127.0.0.1 -p 5432 -U postgres "$db"
          url="postgresql://postgres:postgres@127.0.0.1:5432/${db}"
          echo "HIPICO_E2E_DATABASE_URL=$url" >> "$GITHUB_ENV"
          echo "DATABASE_URL=$url" >> "$GITHUB_ENV"
          echo "HIPICO_OWNER_ID=11111111-1111-4111-8111-111111111111" >> "$GITHUB_ENV"
          echo "HIPICO_SOURCE_GROUP_ID=group-a@g.us" >> "$GITHUB_ENV"
          echo "HIPICO_OPERATOR_CONTROL_TOKEN=ci-shadow-metrics-control-token-0001-strong" >> "$GITHUB_ENV"

      - name: Typecheck exact candidate
        run: npm --workspace backend run typecheck

      - name: Unit and Hípico regressions
        run: npm --workspace backend run test:hipico

      - name: Real PostgreSQL dual-window promotion E2E
        run: npm --workspace backend run test:hipico:agent

      - name: Build backend exact candidate
        run: npm --workspace backend run build

      - name: Diff whitespace gate
        shell: bash
        env:
          PR_BASE_SHA: ${{ github.event.pull_request.base.sha }}
        run: |
          set -euo pipefail
          base="${PR_BASE_SHA:-${HIPICO_CANDIDATE_SHA}^}"
          git diff --check "$base" "$HIPICO_CANDIDATE_SHA"

      - name: Drop isolated database even after failure
        if: always()
        shell: bash
        env:
          PGPASSWORD: postgres
        run: |
          set -uo pipefail
          cleanup_rc=0
          if [ -n "${HIPICO_E2E_DATABASE_URL:-}" ]; then
            db="${HIPICO_E2E_DATABASE_URL##*/}"
            psql -h 127.0.0.1 -p 5432 -U postgres -d postgres -v ON_ERROR_STOP=1 -c "select pg_terminate_backend(pid) from pg_stat_activity where datname='${db}' and pid <> pg_backend_pid();" || cleanup_rc=1
            dropdb -h 127.0.0.1 -p 5432 -U postgres --if-exists "$db" || cleanup_rc=1
          fi
          exit "$cleanup_rc"
