# QA notice

El archivo `QA-CORPUS.txt` es un conjunto de mensajes de prueba, no instrucciones para ejecutar operaciones reales. Durante este gate el grupo fuente carece de ruta de envío, `sourceSendPossible=false` y el backend devuelve `actions: []`.

No usar dinero real ni promover a modo automático por el resultado de este corpus.
