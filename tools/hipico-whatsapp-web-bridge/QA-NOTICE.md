# QA notice

El archivo `QA-CORPUS.txt` es un conjunto de mensajes de prueba, no instrucciones para ejecutar operaciones reales. Durante este gate `HIPICO_ALLOW_SEND=false` y el backend del grupo devuelve `actions: []`.

No usar dinero real ni promover a modo automatico por el resultado de este corpus.
