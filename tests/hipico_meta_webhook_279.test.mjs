import test from 'node:test';
import assert from 'node:assert/strict';
import { readFile } from 'node:fs/promises';
import { __test__ } from '../frontend/api/hipico/whatsapp-webhook.js';
import { __test__ as statusTest } from '../frontend/api/hipico/status.js';
const source=await readFile(new URL('../frontend/api/hipico/whatsapp-webhook.js',import.meta.url),'utf8');const statusSource=await readFile(new URL('../frontend/api/hipico/status.js',import.meta.url),'utf8');const backendWebhook=await readFile(new URL('../backend/src/modules/hipico-bot/hipico-webhook.routes.ts',import.meta.url),'utf8');const backendSecurity=await readFile(new URL('../backend/src/modules/hipico-bot/hipico-meta-security.ts',import.meta.url),'utf8');
test('Meta serverless endpoint is a raw compatibility adapter to the canonical backend',()=>{assert.match(source,/bodyParser:\s*false/);assert.match(source,/proxyCanonicalRequest/);assert.match(source,/\/api\/v1\/hipico-bot\/webhook/);assert.match(source,/x-hub-signature-256/);assert.match(source,/readRawBody/);assert.doesNotMatch(source,/classifyText/);assert.doesNotMatch(source,/supabase\(/);assert.doesNotMatch(source,/HIPICO_META_APP_SECRET/);});
test('Meta verification query parameters are forwarded without reinterpretation',()=>{const path=__test__.webhookPath({query:{'hub.mode':'subscribe','hub.verify_token':'token','hub.challenge':'123'}});const url=new URL(`https://example.test${path}`);assert.equal(url.pathname,'/api/v1/hipico-bot/webhook');assert.equal(url.searchParams.get('hub.mode'),'subscribe');assert.equal(url.searchParams.get('hub.verify_token'),'token');assert.equal(url.searchParams.get('hub.challenge'),'123');});
test('canonical backend retains strong Meta HMAC and constant-time verify-token checks',()=>{assert.match(backendWebhook,/metaSignatureValid\(raw/);assert.match(backendWebhook,/metaVerifyTokenValid\(token\)/);assert.match(backendSecurity,/MIN_META_SECRET_LENGTH=32/);assert.match(backendSecurity,/crypto\.createHmac\('sha256'/);assert.match(backendSecurity,/crypto\.timingSafeEqual/);});
test('canonical backend pins phone number identity and requires durable persistence for real messages',()=>{assert.match(backendWebhook,/WEBHOOK_PHONE_NUMBER_MISMATCH/);assert.match(backendWebhook,/WHATSAPP_PHONE_NUMBER_ID/);assert.match(backendWebhook,/HipicoBotStore\.dbReady\(true\)/);assert.match(backendWebhook,/webhook_persistence_unavailable/);assert.match(backendWebhook,/processMessagesBounded/);});
test('compatibility status uses the same canonical backend webhook configuration names',()=>{const strong='x'.repeat(32);const ready=statusTest.secretReadiness({WHATSAPP_VERIFY_TOKEN:strong,WHATSAPP_APP_SECRET:strong});assert.equal(ready.webhookVerifyTokenStrong,true);assert.equal(ready.webhookAppSecretStrong,true);assert.equal(statusTest.secretReadiness({WHATSAPP_VERIFY_TOKEN:'short'}).webhookVerifyTokenStrong,false);assert.match(statusSource,/\['WHATSAPP_VERIFY_TOKEN', 'WHATSAPP_APP_SECRET', 'WHATSAPP_PHONE_NUMBER_ID'\]/);assert.match(statusSource,/webhookAuthority:\s*'canonical_backend'/);});
test('oversized adapter requests fail closed before proxy and backend owns semantic parsing',()=>{assert.match(source,/request_body_too_large/);assert.match(source,/status\(413\)/);assert.match(source,/retryable:\s*false/);assert.doesNotMatch(source,/JSON\.parse\(raw/);});
test('adapter failure is retryable without logging raw signed payload',()=>{assert.match(source,/canonical_backend_unavailable/);assert.match(source,/console\.error\('hipico canonical webhook proxy failed', \{ message:/);assert.doesNotMatch(source,/console\.error\([^\n]*raw/);assert.doesNotMatch(source,/console\.error\([^\n]*body/);});
