import { PageHeader, Field, Select, Button, Table, Badge, MetricGrid, Section } from '../components/ui/index.js';
import { bs, usd, number, shortDate } from '../core/formatters.js';
import { calculateLedger } from '../core/calculator.js';
import { escapeHtml, mountSubmit, qsa, uid, today, downloadText } from '../utils/dom.js';

const docTypes=[{value:'SA',label:'SA · Asiento diario'},{value:'AB',label:'AB · Documento contable'},{value:'DZ',label:'DZ · Cobranza cliente'},{value:'KZ',label:'KZ · Pago proveedor'},{value:'PR',label:'PR · Nómina'},{value:'AJ',label:'AJ · Ajuste / reclasificación'}];
const templates=[{id:'saleVat',label:'Venta con IVA',debitAccount:'Cuentas por cobrar',creditAccount:'Ventas',description:'Venta comercial con IVA débito fiscal'},{id:'bankExpense',label:'Egreso bancario',debitAccount:'Gastos administrativos',creditAccount:'Bancos',description:'Pago operativo desde banco'},{id:'inventoryPurchase',label:'Compra inventario',debitAccount:'Inventario',creditAccount:'Cuentas por pagar',description:'Compra de inventario / mercancía'},{id:'payroll',label:'Nómina',debitAccount:'Gastos administrativos',creditAccount:'Bancos',description:'Registro de nómina y pago'}];
const normalizeAmount=(value)=>Number(value||0);
const toBs=(amount,currency,rate)=>currency==='USD'?normalizeAmount(amount)*normalizeAmount(rate):normalizeAmount(amount);
const groupDocs=(entries=[])=>entries.reduce((acc,entry)=>{const key=entry.docNo||entry.id||'SIN-DOC';(acc[key]||=[]).push(entry);return acc;},{});
const currencyDual=(amountBs,rate)=>{const bsValue=normalizeAmount(amountBs),usdValue=rate?bsValue/normalizeAmount(rate):0;return `<span class="cg-ledger-dual"><strong>${bs(bsValue)}</strong><small>${usd(usdValue)}</small></span>`;};
function documentRows(entries,rate){return entries.map((entry,index)=>{const debitBs=toBs(entry.debit,entry.currency||'VES',entry.rate||rate),creditBs=toBs(entry.credit,entry.currency||'VES',entry.rate||rate);return `<tr><td class="cg-u-text-mono"><strong>${escapeHtml(entry.docNo||'-')}</strong><br><small>${String(index+1).padStart(3,'0')}</small></td><td>${shortDate(entry.date)}<br><small>FecCont/FecDoc</small></td><td><strong>${escapeHtml(entry.docType||'SA')}</strong><br><small>${escapeHtml(entry.reference||entry.id||'-')}</small></td><td class="cg-u-text-mono">${escapeHtml(entry.account)}</td><td>${escapeHtml(entry.description||entry.headerText||'')}</td><td class="cgx-num">${normalizeAmount(entry.debit)?number(entry.debit):'-'}</td><td>${escapeHtml(entry.currency||'VES')}</td><td class="cgx-num">${debitBs?currencyDual(debitBs,entry.rate||rate):'-'}</td><td class="cgx-num">${creditBs?currencyDual(creditBs,entry.rate||rate):'-'}</td><td>${entry.source==='supabase'?Badge('Supabase','success'):Badge('Local','warning')}</td><td>${Button({text:'Eliminar línea',icon:'fa-trash',variant:'danger',className:'cgx-btn-icon',attrs:`data-delete-ledger="${escapeHtml(entry.id)}" aria-label="Eliminar línea ${index+1}"`})}</td></tr>`;});}
function csvFromEntries(entries=[],rate=0){const header=['docNo','lineNo','date','docType','reference','account','description','debitME','creditME','currency','rate','debitBs','creditBs'];const lines=entries.map((entry,index)=>{const lineNo=entry.lineNo||index+1,currency=entry.currency||'VES',rowRate=entry.rate||rate;return [entry.docNo||'',lineNo,entry.date||'',entry.docType||'',entry.reference||'',entry.account||'',entry.description||'',entry.debit||0,entry.credit||0,currency,rowRate,toBs(entry.debit,currency,rowRate),toBs(entry.credit,currency,rowRate)].map((value)=>`"${String(value??'').replaceAll('"','""')}"`).join(',');});return [header.join(','),...lines].join('\n');}
function htmlBook(state){const rate=Number(state.bcv?.rate||0),entries=state.ledger.entries||[];return `<!doctype html><html lang="es"><head><meta charset="utf-8"><title>Libro diario ContaGest</title><style data-cg-print-only>body{font-family:Inter,Arial,sans-serif;padding:32px;color:#0f172a}h1{font-size:28px;margin:0 0 8px}p{color:#475569;font-weight:700}table{width:100%;border-collapse:collapse;margin-top:20px;font-size:12px}th{background:#e8eef7;text-align:left;color:#1e3a8a;text-transform:uppercase;letter-spacing:.08em}th,td{border:1px solid #cbd5e1;padding:8px}td.num{text-align:right;font-family:Consolas,monospace}.doc{font-weight:900;color:#0b1f3a}</style></head><body><h1>Libro Diario · ${escapeHtml(state.settings?.companyTradeName||state.settings?.companyName||'ContaGest')}</h1><p>RIF ${escapeHtml(state.settings?.companyRif||'')} · Tasa referencial ${bs(rate)} / USD · Generado ${new Date().toLocaleString('es-VE')}</p><table><thead><tr><th>Doc.</th><th>Fecha</th><th>Tipo</th><th>Cuenta</th><th>Texto</th><th>Debe Bs</th><th>Haber Bs</th></tr></thead><tbody>${entries.map((entry)=>`<tr><td class="doc">${escapeHtml(entry.docNo||'')}</td><td>${shortDate(entry.date)}</td><td>${escapeHtml(entry.docType||'SA')}</td><td>${escapeHtml(entry.account||'')}</td><td>${escapeHtml(entry.description||'')}</td><td class="num">${number(toBs(entry.debit,entry.currency||'VES',entry.rate||rate))}</td><td class="num">${number(toBs(entry.credit,entry.currency||'VES',entry.rate||rate))}</td></tr>`).join('')}</tbody></table></body></html>`;}

export const LedgerPage={
  render(state){
    const rate=Number(state.bcv?.rate||0),entries=state.ledger.entries||[];
    const totals=calculateLedger(entries.map((entry)=>({debit:toBs(entry.debit,entry.currency||'VES',entry.rate||rate),credit:toBs(entry.credit,entry.currency||'VES',entry.rate||rate)})));
    const rows=documentRows(entries,rate),accountOptions=(state.ledger.accounts||[]).map((name)=>({value:name,label:name}));
    const actions=`${Button({id:'btnDownloadLedgerCsv',text:'Descargar CSV',icon:'fa-file-csv',variant:'secondary'})}${Button({id:'btnPrintLedgerBook',text:'Vista / imprimir libro',icon:'fa-print',variant:'secondary'})}${Button({id:'btnLedgerTemplateSale',text:'Plantilla venta',icon:'fa-wand-magic-sparkles'})}${Button({id:'btnSyncLedger',text:'Sincronizar',icon:'fa-cloud-arrow-down',variant:'secondary',attrs:'data-admin-only="true"'})}`;
    return `<section class="cg-page-stack cg-ledger-page">${PageHeader({eyebrowKey:'ledgerEyebrow',titleKey:'ledgerTitle',descKey:'ledgerDesc',actions})}${MetricGrid([{label:'Documentos',value:String(Object.keys(groupDocs(entries)).length),hint:'Documentos contables',iconName:'fa-file-invoice',tone:'neutral'},{label:'Líneas',value:String(entries.length),hint:'Movimientos registrados',iconName:'fa-list-ol',tone:'neutral'},{label:'Debe',value:bs(totals.debit),hint:rate?usd(totals.debit/rate):'USD pendiente',iconName:'fa-arrow-up',tone:'brand'},{label:'Haber',value:bs(totals.credit),hint:rate?usd(totals.credit/rate):'USD pendiente',iconName:'fa-arrow-down',tone:'brand'},{label:'Estado',value:totals.balanced?'Balanceado':'Descuadre',hint:totals.balanced?'Partida doble OK':`Diferencia ${bs(totals.diff)}`,iconName:'fa-scale-balanced',tone:totals.balanced?'success':'danger'}])}<section class="cg-ledger-workbench">${Section({title:'Documento contable',subtitle:'Captura guiada con contrapartida automática para generar Debe/Haber en una sola operación.',className:'cg-ledger-form',actions:Badge(totals.balanced?'Libro balanceado':'Requiere revisión',totals.balanced?'success':'danger'),children:`<form id="ledgerForm" class="cg-record-form"><div class="cg-record-fields cg-fields-compact">${Field({labelKey:'Fecha contable',name:'date',type:'date',value:today()})}${Field({labelKey:'Nº documento',name:'docNo',value:`DI-${new Date().getFullYear()}-${String(entries.length+1).padStart(4,'0')}`})}${Select({labelKey:'Clase doc.',name:'docType',options:docTypes})}${Field({labelKey:'Referencia',name:'reference',placeholder:'Factura, banco, nómina...'})}${Select({labelKey:'Moneda',name:'currency',value:'VES',options:[{value:'VES',label:'Bs / VES'},{value:'USD',label:'USD'}]})}${Field({labelKey:'Tasa',name:'rate',type:'number',value:String(rate||0),attrs:'step="0.0001"'})}${Field({labelKey:'Texto cabecera',name:'headerText',className:'cg-field-wide',placeholder:'Resumen del documento contable'})}</div><fieldset class="cg-ledger-line-card"><legend>Línea y contrapartida</legend><div class="cg-record-fields cg-fields-compact">${Select({labelKey:'Cuenta principal',name:'account',options:accountOptions})}${Select({labelKey:'Cuenta contrapartida',name:'counterAccount',options:accountOptions})}${Field({labelKey:'Texto línea',name:'description',className:'cg-field-wide',required:true,placeholder:'Descripción de la operación'})}${Select({labelKey:'Naturaleza principal',name:'side',value:'debit',options:[{value:'debit',label:'Debe'},{value:'credit',label:'Haber'}]})}${Field({labelKey:'Monto',name:'amount',type:'number',attrs:'step="0.01" min="0"',value:'0'})}</div></fieldset><div class="cg-record-actions">${Button({text:'Agregar documento balanceado',icon:'fa-book',type:'submit'})}${Button({id:'btnLedgerCounterpart',text:'Previsualizar contrapartida',icon:'fa-right-left',variant:'secondary'})}</div></form>`})}${Section({title:'Vista previa',subtitle:'Hoja de libro diario antes de registrar.',className:'cg-ledger-preview-card',children:`<div id="ledgerLivePreview" class="cg-ledger-live-preview">Completa el formulario para ver la contrapartida.</div><div class="cg-ledger-template-list">${templates.map((template)=>Button({text:template.label,variant:'secondary',attrs:`data-ledger-template="${template.id}" title="${escapeHtml(template.debitAccount)} ↔ ${escapeHtml(template.creditAccount)}"`})).join('')}</div>`})}</section>${Section({title:'Diario de documentos',subtitle:'Libro Diario operativo con moneda extranjera, moneda local y origen de cada línea.',className:'cg-ledger-book',actions:Badge(totals.balanced?'Libro balanceado':'Libro descuadrado',totals.balanced?'success':'danger'),children:Table({headers:[{label:'Nº doc.'},{label:'FecCont / FecDoc'},{label:'Clase / Referencia'},{label:'Cuenta'},{label:'Texto cabecera doc.'},{label:'Imp. debe ME'},{label:'Mon.'},{label:'Imp. debe ML'},{label:'Imp. haber ML'},{label:'Origen'},{label:'Acciones'}],rows})})}</section>`;
  },
  mount(state,{Store,Toast,SupabaseSyncService}){
    const rate=Number(Store.get().bcv?.rate||0);
    const refreshPreview=()=>{const form=document.getElementById('ledgerForm'),host=document.getElementById('ledgerLivePreview');if(!form||!host)return;const data=Object.fromEntries(new FormData(form)),amount=normalizeAmount(data.amount),rowRate=normalizeAmount(data.rate||rate),mainDebit=data.side==='debit'?amount:0,mainCredit=data.side==='credit'?amount:0,counterDebit=data.side==='credit'?amount:0,counterCredit=data.side==='debit'?amount:0;host.innerHTML=`<div class="cgx-table-wrap"><table class="cgx-table cg-ledger-mini"><thead><tr><th>Cuenta</th><th>Debe</th><th>Haber</th><th>Bs</th></tr></thead><tbody><tr><td>${escapeHtml(data.account||'-')}</td><td>${mainDebit?number(mainDebit):'-'}</td><td>${mainCredit?number(mainCredit):'-'}</td><td>${bs(toBs(mainDebit||mainCredit,data.currency||'VES',rowRate))}</td></tr><tr><td>${escapeHtml(data.counterAccount||'-')}</td><td>${counterDebit?number(counterDebit):'-'}</td><td>${counterCredit?number(counterCredit):'-'}</td><td>${bs(toBs(counterDebit||counterCredit,data.currency||'VES',rowRate))}</td></tr></tbody></table></div>`;};
    document.getElementById('btnSyncLedger')?.addEventListener('click',()=>SupabaseSyncService.pullLedger({Store,Toast,force:true,silent:false}));
    document.getElementById('btnDownloadLedgerCsv')?.addEventListener('click',()=>downloadText(`libro-diario-${today()}.csv`,csvFromEntries(Store.get().ledger.entries,Store.get().bcv.rate),'text/csv;charset=utf-8'));
    document.getElementById('btnPrintLedgerBook')?.addEventListener('click',()=>{const win=window.open('','_blank','noopener,noreferrer');if(!win)return Toast.show('Permite ventanas emergentes para imprimir el libro.','warning');win.document.write(htmlBook(Store.get()));win.document.close();setTimeout(()=>win.print(),250);});
    document.getElementById('btnLedgerCounterpart')?.addEventListener('click',refreshPreview);document.getElementById('ledgerForm')?.addEventListener('input',refreshPreview);document.getElementById('btnLedgerTemplateSale')?.addEventListener('click',()=>document.querySelector('[data-ledger-template="saleVat"]')?.click());
    qsa('[data-ledger-template]').forEach((button)=>button.addEventListener('click',()=>{const template=templates.find((item)=>item.id===button.dataset.ledgerTemplate),form=document.getElementById('ledgerForm');if(!template||!form)return;form.querySelector('[name="account"]').value=template.debitAccount;form.querySelector('[name="counterAccount"]').value=template.creditAccount;form.querySelector('[name="description"]').value=template.description;form.querySelector('[name="headerText"]').value=template.description;form.querySelector('[name="side"]').value='debit';refreshPreview();Toast.show(`Plantilla aplicada: ${template.label}`,'success');}));
    mountSubmit('#ledgerForm',async(data,form)=>{const amount=normalizeAmount(data.amount);if(!amount)return Toast.show('Indica un monto mayor a cero.','warning');if(!data.account||!data.counterAccount)return Toast.show('Selecciona cuenta principal y contrapartida.','warning');if(data.account===data.counterAccount)return Toast.show('La cuenta principal y la contrapartida no pueden ser iguales.','warning');const rowRate=normalizeAmount(data.rate||Store.get().bcv.rate||0),docNo=data.docNo||uid('doc'),base={date:data.date||today(),docNo,docType:data.docType||'SA',reference:data.reference||'',headerText:data.headerText||data.description||'',currency:data.currency||'VES',rate:rowRate,source:'local'},lineA={id:uid('entry'),...base,lineNo:1,account:data.account,description:data.description,debit:data.side==='debit'?amount:0,credit:data.side==='credit'?amount:0},lineB={id:uid('entry'),...base,lineNo:2,account:data.counterAccount,description:`Contrapartida: ${data.description}`,debit:data.side==='credit'?amount:0,credit:data.side==='debit'?amount:0};try{const savedA=await SupabaseSyncService.createLedgerEntry(lineA),savedB=await SupabaseSyncService.createLedgerEntry(lineB);Store.update((draft)=>{draft.ledger.entries=[savedB,savedA,...(draft.ledger.entries||[])];});Toast.show('Documento contable guardado en Supabase.','success');}catch(error){Store.update((draft)=>{draft.ledger.entries=[lineB,lineA,...(draft.ledger.entries||[])];draft.auditLog.unshift({id:uid('log'),module:'ledger',action:'create-balanced-document-local',at:new Date().toISOString(),docNo});});Toast.show(`Documento balanceado guardado localmente. Backend: ${error.message}`,'warning');}form.reset();form.querySelector('[name="date"]').value=today();form.querySelector('[name="docNo"]').value=`DI-${new Date().getFullYear()}-${String((Store.get().ledger.entries||[]).length+1).padStart(4,'0')}`;form.querySelector('[name="rate"]').value=String(Store.get().bcv?.rate||0);refreshPreview();});
    qsa('[data-delete-ledger]').forEach((button)=>button.addEventListener('click',()=>Store.update((draft)=>{draft.ledger.entries=draft.ledger.entries.filter((entry)=>entry.id!==button.dataset.deleteLedger);})));refreshPreview();
  }
};
