{
  "version": "erp-release-governance-v97.1",
  "repository": "adelvisMartin/ContaGest",
  "branch": "main",
  "phase": "STRUCTURAL_UNTIL_CI_134_RECOVERS",
  "structural": {
    "requirePullRequest": true,
    "requiredApprovingReviewCount": 0,
    "requireConversationResolution": true,
    "allowForcePushes": false,
    "allowDeletions": false,
    "enforceAdmins": true
  },
  "requiredChecks": {
    "activeNow": [],
    "pendingActivationAfterIssue134": [
      "ContaGest CI / validate"
    ],
    "futureCandidates": [
      "PostgreSQL real gate",
      "Browser QA"
    ],
    "rule": "Never configure a required check that has not demonstrated reliable runner-backed execution on the repository."
  },
  "breakGlass": {
    "default": "DENY",
    "permanentAdminBypass": false,
    "requirements": [
      "document incident and candidate SHA",
      "record why normal PR/check path is blocked",
      "obtain explicit owner decision",
      "restore protection immediately after emergency",
      "run deferred QA and attach evidence"
    ]
  },
  "truthStates": ["PASS", "FAIL", "BLOCKED", "NOT_EXECUTED"],
  "notes": [
    "Phase A can be activated even while #134 is open because it does not require a broken status check.",
    "Phase B must add real required checks only after #134 demonstrates runner-backed executions.",
    "Repository settings remain the runtime authority; this JSON is declarative intent, not proof that protection is active."
  ]
}
