# QA Today — 2026-08-30

Este runbook existe porque #134 puede impedir que GitHub Actions asigne runner. **No sustituye Actions ni convierte un job sin steps en PASS**; permite ejecutar hoy la misma disciplina de evidencia desde una máquina local.

## 1. Preparación

Desde la raíz del repositorio y sobre el commit exacto que vas a probar:

```bash
git pull --ff-only
npm ci
npx playwright install chromium
npm run qa:today
npm run qa:today:prepare -- --scope=all
```

`qa:today:prepare` crea solamente templates `NOT_EXECUTED`; no inventa resultados.

## 2. ContaGest ERP

### Source/unit/build

```bash
npm run lint
npm test
npm run build
```

### 58 rutas / responsive / composición

```bash
npm run test:browser:58
npx playwright test qa/erp-mobile-58-v181.spec.mjs --project=chromium --workers=1
npm run qa:a11y
```

### Golden financiero

```bash
CANDIDATE_SHA=$(git rev-parse HEAD) node scripts/erp-financial-golden-v156.mjs
```

### Performance #157

Completa mediciones reales en `artifacts/qa/erp-performance-v157/<SHA>/measurements.json` para cold, warm, navegación repetida, sesión larga y red limitada. Luego:

```bash
CANDIDATE_SHA=$(git rev-parse HEAD) node scripts/erp-performance-gate-v157.mjs check
```

### Deploy servido/PWA #182

```bash
CANDIDATE_SHA=$(git rev-parse HEAD) \
ERP_PRODUCTION_URL=https://TU-DEPLOY \
node scripts/erp-mobile-production-gate-v182.mjs probe
```

Para la matriz real servida usa una sesión QA, no credenciales reales de cliente:

```bash
ERP_PRODUCTION_URL=https://TU-DEPLOY \
ERP_QA_SESSION_JSON='<SESION_QA_JSON>' \
CANDIDATE_SHA=$(git rev-parse HEAD) \
npx playwright test qa/erp-mobile-production-v182.spec.mjs --project=chromium --workers=1
```

No guardes la sesión QA en Git ni en screenshots.

## 3. Control Hípico

### Automated/local

```bash
npm run test:hipico
npm run qa:hipico
npm run qa:hipico:visual
```

### Physical QA #119

```bash
npm run qa:today:prepare -- --scope=hipico
```

Edita **durante la prueba**, no antes:

`artifacts/qa/hipico-v119/<SHA>/physical-qa.json`

SOURCE debe permanecer read-only. LAB es el único destino de escritura del gate. Ejecuta la matriz: fresh install, upgrade, background/resume, kill, reboot, permisos, offline/reconnect, dos sesiones, QR/session recovery, cambio de identidad visible del grupo, dos jornadas SOURCE→parse→prediction→LAB, scroll/overlap, batería/memoria, backup/restore y handoff/restart.

Al terminar:

```bash
node scripts/hipico-physical-qa-v119.mjs status
node scripts/hipico-physical-qa-v119.mjs check
```

### Soak #120

El soak final debe durar >=24h. Ejemplo seguro:

```bash
GIT_SHA=$(git rev-parse HEAD) npm --workspace backend run soak:hipico -- \
  --duration-minutes=1440 \
  --source-read-only=PASS \
  --lab-only-write=PASS
```

Los recovery drills requeridos deben ejecutarse realmente y pasarse por sus flags; no marques PASS sólo porque el proceso sobrevivió.

## 4. WhatsApp Production #154 / Promotion #121

A fecha 2026-08-30 la política oficial de WhatsApp Business continúa listando **apuestas con dinero real** entre los bienes/servicios regulados o restringidos cuyo intercambio no puede facilitarse mediante los Servicios de WhatsApp Business. Por ello el gate productivo continúa `NO_GO` y #121 debe mantener `shadow`.

Esto **no impide el QA físico seguro** de hoy: SOURCE read-only + LAB write-only + simulador local. Sí impide interpretar un test exitoso como autorización para que el bot responda automáticamente en el grupo de apuestas real.

## 5. Estado final

```bash
npm run qa:today:status -- --scope=all
```

Interpretación obligatoria:

- `PASS`: caso realmente ejecutado y conforme;
- `FAIL`: defecto reproducible;
- `BLOCKED`: dependencia externa/infraestructura impidió ejecutar;
- `NOT_EXECUTED`: aún no ejecutado;
- nunca convertir BLOCKED/NOT_EXECUTED en PASS.

## 6. #134

Si GitHub Actions sigue mostrando `runner_id=0`, `runner_name=""`, `steps=[]`, conserva #134 abierto. Cuando vuelva el runner, re-ejecuta al menos workflow mínimo, ContaGest CI y PostgreSQL real sobre el SHA final y adjunta esa evidencia antes de cerrar el incidente.
